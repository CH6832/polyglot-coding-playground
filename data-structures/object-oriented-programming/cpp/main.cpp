#include <iostream>
#include <vector>
#include "Shape.h"
#include "Rectangle.h"
#include "Circle.h"

int main() {
    // Using Inheritance
    Rectangle rectangle(5, 3);
    Circle circle(4);

    // Using Composition
    std::vector

    // Using Inheritance
    Rectangle rectangle(5, 3);
    Circle circle(4);

    // Using Inheritance
    Rectangle rectangle(5, 3);
    Circle circle(4);

    // Using Composition
    std::vector```
